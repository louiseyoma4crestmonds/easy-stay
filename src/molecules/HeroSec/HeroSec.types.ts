export type HeroSecProps = {
  userAuthenticated?: boolean;
  userDetails?: { firstName: string; lastName: string };
  isLoggedIn?: boolean;
  firstName?: string;
  lastName?: string;
  points?: number;
};
